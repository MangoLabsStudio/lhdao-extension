# Third-party notices

`tlsn-wasm@0.1.0-alpha.15` is bundled for the Lighthouse zkTLS runtime. Its
package metadata declares `MIT OR Apache-2.0`; Lighthouse elects Apache-2.0.
The upstream package is published by [TLSNotary](https://github.com/tlsnotary/tlsn).

The zkTLS integration follows the TLSNotary extension runtime pattern. It does
not include a TLSNotary verifier or proxy service.
